import 'package:flutter/material.dart';

class ViewReferralScreen extends StatelessWidget {
  const ViewReferralScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
